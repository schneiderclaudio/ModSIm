"""ModSIM - mineral processing plant simulator GUI.

A cross-platform PySide6 (Qt6) desktop application that replaces the legacy
VB6 GUI. It preserves the legacy text job-file format and calls the 64-bit
Fortran engine via ``ctypes``.
"""

__version__ = "0.1.0"
